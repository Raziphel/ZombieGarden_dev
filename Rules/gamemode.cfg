# config file for game rules
# rules name: Zombie Garden
# rules description:

teams                = team1.cfg; team2.cfg;

gamemode_name        = Zombie Garden
gamemode_info        = Welcome to Zombie Garden.\nThe great return is here!

scripts              = KAG.as;
					   old_net.as;
					   Zombies_Main.as;
					   Zombies_Interface.as;
					   CTF_Trading.as;
					   UseFakeTechs.as;
					   SpawnFish.as;
					   SpawnMigrants.as;
					   TeamMenuJustSpectator.as;
					   SpawnTraders.as;
					   CTF_GiveSpawnItems.as;
					   CTF_Structs.as;
					   # CTF_BuilderMenu.as;                # disabled
					   KillMessages.as;
					   JoinCoreHooks.as;
					   CoreHooks.as;
					   CTF_PickSpawn.as;
					   ChatCommands.as;
					   TimeToEnd.as;
					   RestartAfterShortPostGame.as;
					   FanfareOnWin.as;
					   PlayerCamera.as;
					   DefaultScoreboard.as;
					   Editor.as;
					   SpawnImmunity.as;
					   BasicHelps.as;
					   ShowGamemode.as;
					   VoteCore.as;
					   DefaultVotes.as;
					   BasePNGLoader.as;
					   GenerateFromKAGGen.as;
					   MechanismsServer.as;
					   MechanismsReader.as;
					   ScoreboardRender.as;
					   Holiday.as;
					   WheelMenu.as;
					   EmoteMenu.as;
					   EmoteBinderMenu.as;
					   # TauntMenu.as;                      # disabled to prevent console spam
					   BindingsMenu.as;
					   BloodyTileSetter.as;
					   RegrowPlants.as;
					   WeatherSystem.as;


daycycle_speed       = 3.0          # 1 day = X minutes; 0 = no cycle
daycycle_start       = 0.3           # 0.0 = midnight; 0.5 = midday; 1.0 = midnight

autoassign_teams     = no            # team allocation is not smart in this gamemode
auto_bots            = no
no_shadowing         = 0

attackdamage_modifier   = 0.50
friendlydamage_modifier = 0.0

restartmap_onlastplayer_disconnect = yes
mirrormap              = yes
minimap                = yes

room_config           =

support_factor = 24
support_added_vertical = 1
support_cost_castle = 4
support_cost_wood = 3


map_fire_update_ticks = 7
